pandas>=2.0
pyarrow>=14.0
numpy>=1.24
requests>=2.31
boto3>=1.34
pandas_market_calendars>=4.4
numba>=0.60
# optional second signal for the split detector (alert path only; a handful of calls a month;
# import is guarded, absence leaves the alert behaviour unchanged)
yfinance>=0.2.60
